rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /guests/{token} {
      allow read, write: if true; // cambiar en producción final
    }
    match /songs/{doc} {
      allow create: if true;
      allow read: if false; // solo el admin lo lee
    }
  }
}